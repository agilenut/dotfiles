# prompt.zsh
# =============================================================================
# Setup the prompt.

eval "$(oh-my-posh init zsh \
  --config ${XDG_CONFIG_HOME:-$HOME/.config}/oh-my-posh/themes/zen-2.omp.toml)"
