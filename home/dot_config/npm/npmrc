# Make XDG compliant
cache=${XDG_CACHE_HOME}/npm
init-module=${XDG_CONFIG_HOME}/npm/config/npm-init.js
logs-dir=${XDG_STATE_HOME}/npm/logs

# Disable funding message.
fund=false

# Less chatty
loglevel=warn

# Skip default security audits
#audit=false
