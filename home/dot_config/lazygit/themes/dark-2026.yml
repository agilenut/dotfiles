promptToReturnFromSubprocess: false
os:
  editPreset: "nvim"
gui:
  sidePanelWidth: 0.25
  scrollHeight: 8
  nerdFontsVersion: "3"
  # Match delta's filename header (named ANSI blue) so accents share one palette.
  theme:
    activeBorderColor:
      - blue
    inactiveBorderColor:
      - "#8b949e"
    selectedLineBgColor:
      - "#253748"
    selectedRangeBgColor:
      - "#253748"
    optionsTextColor:
      - blue
  authorColors:
    "agilenut": blue # your commits in the theme blue
    "*": "#8b949e" # everyone else muted
  branchColorPatterns:
    "^feat/": "green"
    "^fix/": "red"
    "^refactor/": "magenta"
    "^perf/": "magenta"
    "^docs/": "blue"
    "^test/": "yellow"
    "^ci/": "cyan"
    "^build/": "cyan"
    "^chore/": "#a3a9ae"
    "^style/": "#a3a9ae"
git:
  diffContextSize: 10
  pagers:
    - colorArg: always
      pager: delta --paging=never
