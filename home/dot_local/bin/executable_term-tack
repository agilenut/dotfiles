#!/bin/bash
# ~/bin/term-tack

# @raycast.schemaVersion 1
# @raycast.title Tack Terminal
# @raycast.mode silent
# @raycast.icon 💻
# @raycast.packageName Terminals

exec "$HOME/.local/bin/term-launch" tack \
  "$HOME/.config/alacritty/tack.toml" "$HOME/repos/tack" "Tack"
