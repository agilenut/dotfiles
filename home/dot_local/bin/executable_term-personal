#!/bin/bash
# ~/bin/term-personal

# @raycast.schemaVersion 1
# @raycast.title Personal Terminal
# @raycast.mode silent
# @raycast.icon 💻
# @raycast.packageName Terminals

exec "$HOME/.local/bin/term-launch" personal \
  "$HOME/.config/alacritty/alacritty.toml" "$HOME/repos/" "Personal"
