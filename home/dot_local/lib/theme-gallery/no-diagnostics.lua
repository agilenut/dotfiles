-- theme-gallery: disable diagnostic display so LSP diagnostic signs (e.g.
-- unused-variable hints) don't occupy the sign column. Used for the gitsigns
-- showcase, where the gitsigns add/change/delete signs are the point.
vim.diagnostic.enable(false)
